<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.samplemysqlapi.mapper.UsersMapper">  <!-- (1) -->
    <select id="findUserList" resultType="com.example.samplemysqlapi.mapper.entity.UsersEntity">  <!-- (2) -->
        select
        user_id,
        user_name
        from
        users
    </select>
</mapper>